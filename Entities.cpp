//
// Created by ALIN on 26.02.2022.
//

#include "Entities.h"

Entities::Entities(int hp, int mana, int atk, int dex, int def) : hp(hp), mana(mana), atk(atk), dex(dex), def(def) {}

Entities::Entities() :hp(0), mana(0), atk(0), dex(0), def(0){

}
